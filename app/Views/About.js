import React from 'react';
import {
  StyleSheet,
  Text,
} from 'react-native';

const About = () => {
  return <Text style={styles.header}>Aboute</Text>
};

const styles = StyleSheet.create({
  header: {
    fontSize: 20
  },
});

export default About;
