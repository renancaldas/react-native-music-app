import React from 'react';
import {
  StyleSheet,
  Text,
} from 'react-native';

const About = () => {
  return <Text style={styles.header}>Homele</Text>
};

const styles = StyleSheet.create({
  header: {
    fontSize: 20
  },
});

export default About;
